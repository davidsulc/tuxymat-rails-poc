en:
  activerecord:
    models:
      vending_machine: Vending machine
    attributes:
      vending_machine:
        age_verification: "Age verification"
        purchase_price: "Purchase price"
        serial_number: "Serial number"
    errors:
      models:
        vending_machine:
          attributes:
            serial_number:
              taken: "already exists"
            purchase_price:
              invalid: "max 2 digits allowed after '.'"
              less_than: "must be less than %{count}"
              greater_than: "must be greater than %{count}"
        
  vending_machines:
    create:
      message: Create new vending machine
      success: Vending machine was successfully created.
    update:
      message: Update vending machine
      success: Vending machine was successfully updated.
    delete:
      success: Vending machine was successfully deleted.
    new: New vending machine
    editing: Editing vending machine
    none_to_display: There are no vending machines to display.
    purchase_price_hint: "If it is entered, it must be between 0 and 10'000"
    serial_number_hint: "Must be unique"
        